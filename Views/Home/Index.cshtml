@* @{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome M.I.S.S</h1>

    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>

@{
    var logoutMessage = TempData["LogoutMessage"] as string;
}

@if (!string.IsNullOrEmpty(logoutMessage))
{
    <script>
        alert("@logoutMessage");
    </script>
}
 *@

@{
    ViewData["Title"] = "Home Page";
}

<style>
    .background-image {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
        opacity: 0.6; 
        background: url('@Url.Content("~/images/background2.png")') no-repeat center center fixed;
        background-size: cover;
    }

    .display-4 {
        position: relative;
        /* 원하는 위치로 조정하기 위한 추가 스타일 */
        top: 100px; /* 예시: 텍스트를 아래로 50px 이동 */
        right: 400px;
        font-weight: 800; /* 글씨 두께를 더 굵게 설정 */
        color: #333; /* 글씨 색상을 어두운 색으로 변경 */
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3); /* 약간의 그림자 추가로 더 두드러지게 설정 */
    }
</style>

<div class="background-image"></div>

<div class="text-center">
    <h1 class="display-4">Welcome M.I.S.S</h1>
</div>

@{
    var logoutMessage = TempData["LogoutMessage"] as string;
}

@if (!string.IsNullOrEmpty(logoutMessage))
{
    <script>
        alert("@logoutMessage");
    </script>
}
